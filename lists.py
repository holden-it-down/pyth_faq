

results = ["Jack", "Lassig"]

# Append to the end of a list
results.append("Kazu")

# removes an item from a list, only removes the first Kazu from the list
results.remove("Kazu")


# adds individual items of a list to a list
results.extend(["Mark", "Jack"])

# Inserting an element to the index you specify
results.insert(0, "John")

# Flips the order of a result
result.reverse()
